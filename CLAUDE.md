# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a newly initialized repository with no application code yet. It is located under `~/Documents/dsq/`, which is configured to use the `dsq` Git identity (`includeIf` in global `.gitconfig`).

## Repository

- **Remote:** `git@github.com:18171191287dsq-debug/git.git` (SSH)
